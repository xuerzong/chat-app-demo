import 'package:flutter/material.dart';

class Constants {
  static OutlineInputBorder boder = OutlineInputBorder(
    borderRadius: BorderRadius.circular(10.0),
    borderSide: BorderSide(color: Colors.white),
  );
}